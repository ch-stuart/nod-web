# sleep-noise-web

Marketing website for [Nod](https://ch-stuart.github.io/nod-web/).

## Develop

```sh
pnpm dev
pnpm build
pnpm preview
```

## Deploy

`pnpm build` outputs to `docs/`. Commit and push — GitHub Pages serves from the `/docs` folder on `main`.
